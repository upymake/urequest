[pytest]
markers =
    unittest: unit tests marker
testpaths = tests/
python_files=*.py
python_functions=test_*
addopts = -rsxX
          -v
          -l
          --emoji-out
          --tb=long
          -m unittest
          --cov=urequest
          -p no:warnings
